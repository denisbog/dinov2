# dinov2

`local_debug.py` contains classes from transformers
`simple_debug.py` running original transformers model

## used coverage refort for cleaup

```sh
coverage run --branch --source . local_debug.py 
coverage html -i
```
